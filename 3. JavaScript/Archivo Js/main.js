/*
function saludar(nombre) {
    let mensaje = `Hola ${nombre}`;
}

let saludar = (nombre) => {
    let mensaje = `Hola ${nombre}`;
}

let Persona = {
    nombre: 'Juan',
    apellido: 'Perez',
    edad: 25,
    getNombre: function() {
        return `${Persona.nombre} ${this.apellido}`;
    }
};


let nombre = "Juan";
let apellido = 'Perez';
let nombreCompleto = `Su nombre es: ${nombre} y su apellido es: ${apellido}`;

let personas= ["Wilson", "Jhonatan", "Rodolfo"];
primerConectado = personas[2];

personas.push("Ana");
for (let i=0; i < personas.length; i++){
    console.log(personas[i]);

}
*/